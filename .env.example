# Optional local overrides. The app reads the Flask API URL from
# `src/utils/constants.ts` by default (http://127.0.0.1:8000).
#
# If you introduce Vite env variables later, copy this file to `.env.local`
# (see Vite docs). `.env` and `.env.local` are gitignored.
