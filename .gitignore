# Build / PyInstaller
backend/build/
backend/dist/
backend/*.spec.bak

# Python / local runs
backend/__pycache__/
**/__pycache__/
*.py[cod]
backend/*.log
.venv/

# Frontend
node_modules/
dist/
*.tsbuildinfo

# Rust / Tauri
src-tauri/target/

# Env (keep .env.example tracked)
.env
.env.local

# Tauri sidecar binaries (per-host triple; generated by npm run sidecar:prepare)
src-tauri/binaries/sdm-api-*
